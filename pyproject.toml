[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mypkg"
version = "0.2.0"
description = "A tiny package for testing DocuPatch's context-aware repair."
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
